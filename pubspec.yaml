name: adminkarieku
description: KarierKu Admin Dashboard - Flutter Web

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0        # ← downgrade dari ^3.6.0
  cloud_firestore: ^4.17.5      # ← downgrade dari ^5.4.0
  firebase_auth: ^4.20.0        # ← downgrade dari ^5.3.0
  intl: ^0.19.0
  file_picker: ^8.0.0
  excel: ^4.0.6
  pdf: ^3.11.1   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
     uses-material-design: true
     assets:
       - assets/images/app_icon.png
       - assets/images/logo1.png